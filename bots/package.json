{
  "name": "@stockey/bots",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "build": "mkdir -p dist && cp -R src dist"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv": "^16.4.5",
    "pg": "^8.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {}
}